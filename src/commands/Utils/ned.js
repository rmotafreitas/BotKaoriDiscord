const ned = require("../../models/ned");

module.exports = {
  name: "ned",
  /**
   * @param {Client} client
   * @param {Message} message
   * @param {String[]} args
   */
  run: async (client, message, args) => {},
};
